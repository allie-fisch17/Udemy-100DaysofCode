#get the input for both hight and weight of user

height = float(input("enter your height in m: "))
weight = float(input("enter your weight in kg: "))

#use mathematical equation to calculate bmi

bmi = round(weight / height ** 2)

#use if,elif,else conditional statements to determine health status of user

if bmi < 18.5:
    print(f"Your bmi is {bmi}, you are underweight.")
elif bmi < 25:
    print(f"Your bmi is {bmi}, you are a normal weight.")
elif bmi < 30:
    print(f"Your bmi is {bmi}, you are slightly overweight.")
elif bmi < 35:
    print(f"Your bmi is {bmi}, you are obese.")
else :
    print(f"Your bmi is {bmi}, you are clinically obese.")
